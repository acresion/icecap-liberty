version: '3.4'

services:
  icecap-nodejs:
    image: icecap-api
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 3000:9080
      - 9229:9229
    
